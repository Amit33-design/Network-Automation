! ============================================================
! NetDesign AI — NX-OS DAY 0 POAP BOOTSTRAP CONFIG
! Device  : {{ hostname }}
! Serial  : {{ serial }}
! Role    : {{ role }}
! Generated by NetDesign AI ZTP/POAP Server
! ============================================================

hostname {{ hostname }}

!-- Enable features needed for day-0
feature lldp
feature ssh

!-- Disable telnet
no feature telnet

!-- Local admin (replace placeholders before production)
username admin password {{ admin_password }} role network-admin
username netdesign password {{ netdesign_password }} role network-admin

!-- Management VRF
vrf context management
  ip route 0.0.0.0/0 {{ mgmt_gw }}

!-- Management interface
interface mgmt0
  description MGMT-ZTP-POAP
  vrf member management
  ip address {{ mgmt_ip }}/24
  no shutdown

!-- Loopback
interface loopback0
  description ROUTER-ID
  ip address {{ loopback_ip }}/32
  no shutdown

!-- NTP
ntp server {{ ntp_server }} use-vrf management prefer

!-- Syslog
logging server {{ syslog_server }} 6 use-vrf management

!-- SSH keys
no ip domain-lookup
ip domain-name {{ hostname | lower }}.ztp.local

!-- Banner
banner motd ^
*** NetDesign AI NX-OS — ZTP/POAP Bootstrap Complete ***
*** {{ hostname }} | {{ role }} ***
^

end
